import 'package:flutter/material.dart';

class CustomUnderLineButtonWidget extends StatelessWidget {
  const CustomUnderLineButtonWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
